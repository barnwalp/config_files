"Setting encoding to UTF-8 to show glyphs
set encoding=UTF-8

"Changing the default font-family and fontsize
"set guifont=Hack:h11
set guifont=Consolas:h11

"Removing the menubar
set guioptions -=m

"Removing the toolbar
set guioptions -=T

"Removing the scrollbar
set guioptions -=r

"Changing the titlestring of GVIM
set title titlestring=VIM

"GVIM does not change its position (become unsnapped in Windows)
set guioptions-=L

"Changing the default directory of GVIM
cd C:\Users\panka\github_repo
